name: Deploy Sanity Studio

on:
  push:
    branches:
      - main
    paths:
      - 'src/sanity/**'
      - 'sanity.config.ts'
      - 'sanity.cli.ts'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Sanity
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          version: 11.22.0

      - uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'

      - uses: ./.github/actions/pnpm-install
        with:
          matchday-github-token: ${{ secrets.MATCHDAY_GITHUB_TOKEN }}

      - name: Deploy to Sanity
        run: pnpm run deploy:sanity -- --yes
        env:
          SANITY_AUTH_TOKEN: ${{ secrets.SANITY_AUTH_TOKEN }}
          NEXT_PUBLIC_SANITY_PROJECT_ID: ${{ vars.SANITY_PROJECT_ID }}
          NEXT_PUBLIC_SANITY_DATASET: ${{ vars.SANITY_DATASET }}
          SANITY_STUDIO_SITE_URL: ${{ vars.SANITY_STUDIO_SITE_URL }}
